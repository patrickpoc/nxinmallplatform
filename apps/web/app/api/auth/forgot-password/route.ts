import { NextResponse } from "next/server";

/** Placeholder endpoint — connects to Resend + verification tokens in production. */
export async function POST() {
  return NextResponse.json({ success: true });
}
