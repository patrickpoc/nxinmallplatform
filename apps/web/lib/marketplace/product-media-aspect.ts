/** Shared aspect for PDP gallery main frame. */
export const PRODUCT_MEDIA_ASPECT_CLASS = "aspect-[3/4]";

/** Purchase card: natural height (no fixed aspect — avoids action row overlap). */
export const PRODUCT_PURCHASE_CARD_ASPECT_CLASS = "w-full";
